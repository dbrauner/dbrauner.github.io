<div>
	{{#each path="notes"}}
	<ul style="margin:0; padding:0; font-size:small;">
		<li><b>[{{text path="type"}}]</b> {{text path="text"}}
			{{#each path="references"}}
				<b>({{text path="type"}} : {{text path="reference"}})</b>
			{{/each}}
		</li></ul>
	{{/each}}
</div>